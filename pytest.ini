[pytest]
testpaths = tests
markers =
    e2e: live integration tests against a real backend (require GRAMPS_MCP_E2E=1 + GRAMPS_MCP_USERNAME/PASSWORD/INSTANCES). Local-only: results contain real user data and must never run in public CI.
    manual: manual smoke tests run outside CI.
addopts = -ra